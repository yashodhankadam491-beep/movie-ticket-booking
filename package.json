{
  "name": "ibox-cvrs-movie-booking",
  "version": "1.3.0",
  "description": "IBOX x CVR's movie booking system with secure authentication, dynamic Maharashtra cinema directory, automated TMDb movie updates, temporary seat locking and Razorpay real payments.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env --require ./payment-hook.js server.js",
    "dev": "node --env-file=.env --require ./payment-hook.js --watch server.js",
    "backup": "node --env-file=.env scripts/backup-db.js",
    "movies:update": "node --env-file=.env scripts/movie-updater.js"
  },
  "dependencies": {
    "node-cron": "^4.6.0",
    "razorpay": "^2.9.8"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
